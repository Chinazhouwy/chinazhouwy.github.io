# 美团后端面经（三面合并）

- **来源**: 小红书
- **noteId**: 69fe086200000000350206fb
- **标签**: `美团` `后端` `Java` `AI` `RAG` `Agent` `三面`
- **考点分类**: AI工作流/Java高并发/MySQL/Redis/分布式锁

---

## 一面

### 1. AI 工作流与 Agent 类项目

- 如何进行市面竞品调研？项目的核心创新点和竞争优势体现在哪里？
- 调用的 LLM 接口格式是什么？是否涉及多模态？
- 基于 Langchain 编排工作流的核心逻辑是什么？
- 调整工作流分支或业务逻辑时，如何控制迭代稳定性，避免影响其他场景？
- 大模型应用类项目测试时重点关注哪些指标（除了 token 数、耗时、准确率外）？
- 如何排查工作流分支链路异常（如模块被跳过、函数忽略）的问题？是否借助 AI 工具？
- 如何理解 Workflow 和 Agent 两个概念？二者的区别是什么？长期来看是否会并存？
- 工作流中是否设计了状态机？出于什么考量？
- RAG 模块中向量数据库的选型标准是什么？数据部署方式、向量维度及存储结构分别是什么？
- RAG 数据库召回时，如何选择最佳的参数组合？双路召回的具体实现和分数重排规则是什么？
- RAG 召回时，如何区分高度相似但关键细节不同的文本？
- 进一步提升大模型应用准确率的瓶颈通常在哪里？是数据集缺陷还是模型能力限制？
- 若将项目场景拓展到其他垂直领域，会遇到哪些问题与适配难点？

### 2. Java 后端高并发项目

- 详细介绍该 Java 项目的架构、核心能力以及最初的开发目的
- 项目中高并发秒杀逻辑的实现方案是什么？核心技术栈（如 Redisson、Lua 脚本、MQ）是如何配合的？
- 项目上线后实际的流量情况如何？日常 QPS 是多少？主要流量集中在查询还是操作接口？
- Redis 的基本数据结构有哪些？介绍 Zset 的特点及典型应用场景
- Redis 的持久化机制有哪些？如何理解缓存的持久化及缓存重建？
- 解释 MySQL 中回表查询的概念
- 该项目开发中，手写代码和 AI 生成代码的占比分别是多少？

## 二面

### 一、个人情况与求职意向类

- 做一下自我介绍（含教育背景、项目经历、技能掌握等）
- 对工作城市有什么偏好？
- 之前有过相关实习经历吗？
- 什么时候毕业？毕业前能否全职实习？面试通过后多久能入职？
- 目前所在地在哪里？学校课程对实习有无影响？

### 二、项目相关类

- 从你的项目中选一个你觉得有意思或成长最多的，详细介绍一下
- 该项目中遇到的最大困难是什么？如何解决的？
- 为什么选择当前使用的大模型开发框架（或工作流框架）？有调研过其他同类框架吗？
- 不同的 Agent 编排框架（如链式调用与图结构调用）的区别是什么？

### 三、MySQL 相关

- 索引执行分析：给定含联合索引（A、B、C、D）的表，以及含条件（A=1、B=2、D=4）的查询语句，分析该 SQL 在索引上的大体执行过程（含索引机制、回表方式等）
- MySQL 中的最左前缀匹配规则是什么？
- 了解索引下推机制吗？
- MySQL 中的锁有哪些？（如记录锁、间隙锁、临键锁）
- 意向锁相关知识有了解吗？
- 临键锁的作用是什么？

### 四、Java 基础与中间件类

- 有看过 Java 集合相关源码吗？比如 HashMap、ConcurrentHashMap
- ConcurrentHashMap 底层扩容时，能否并发扩容？
- ConcurrentHashMap 扩容的大体过程是什么？扩容迁移数据时如何保证安全性？

### 五、Redis 与算法

- Redis 主要用在哪些场景？（如高并发秒杀、分布式锁、缓存等）
- 为什么选择 Redis+Lua 脚本实现分布式锁？
- Redis+Lua 脚本实现分布式锁可能会出现什么问题？（例如：线程 A 锁过期后，线程 B 获取锁，A 操作完成后是否会误删 B 的锁？如何解决？）
- 使用 Redis 做缓存时，采用的缓存策略是什么？
- 如何保证二级缓存与数据源的数据一致性？缓存更新时的操作顺序是怎样的？
- 算法手撕：力扣 445 两数相加 II（中等）

## 加签面

- RAG 检索命中率低如何处理
- 长上下文管理策略
- 多 Agent 与单 Agent 对比
- Agent 记忆管理策略
- ES 主从分片策略及数据一致性如何保证
- 大 Key 问题解决策略
- 深分页问题如何解决
- ConcurrentHashMap 相关八股吟唱
- 分布式锁 Redisson 对比 setnx 优势
- MySQL 隔离级别相关八股
- AI coding 个人风格叙述
