---
title: "最小二乘法"
date: "2026-09-01"
domain: "学习"
area: "数学"
module: "线性代数合集"
project: ""
type: "文章"
status: "可复习"
priority: "P1"
energy: "medium"
visibility: "public"
summary: "最小二乘法 由于最小二乘法在最近出现的频率比较高，所以单独拎出来研究研究，b站上有个几何的解读，虽然感觉弄得不错，但是还是觉得有点深了，而且有点快，各种公式的嵌套看的有点乱，于是乎，记录下自己的一些理…"
tags:
  - 数学
  - 最小二乘法
source: "https://mp.weixin.qq.com/s/kn4izuFPINuRk_SyqN9hAw"
album_position: 12
source_url: "https://mp.weixin.qq.com/s?__biz=MzUzNzY3NjEwNQ==&mid=2247484547&idx=1&sn=e3c485c323973397aae50c9cf04840d5&chksm=fae214a5cd959db3689f90a67aa68f249cd86948c9f3751a06ec899b67db56ee67f68728e6e2"
---
# 最小二乘法

最小二乘法
由于最小二乘法在最近出现的频率比较高，所以单独拎出来研究研究，b站上有个几何的解读，虽然感觉弄得不错，但是还是觉得有点深了，而且有点快，各种公式的嵌套看的有点乱，于是乎，记录下自己的一些理解与看到的东西。
定义
“二乘” ：在古汉语和数学术语中，“二乘”就是平方的意思。
“最小” ：指的是我们要让某个数值达到最小。
一句话定义：
最小二乘法是一种数学优化技术，它通过最小化误差的平方和，来寻找数据的最佳函数匹配。
假设你在纸上画了很多个散点，现在你想画一条直线穿过它们，要求这条直线能最能代表这些点的趋势。你怎么确定哪条线是“最好”的？最小二乘法告诉你：那条让所有点到直线的垂直距离的平方加起来最小的线，就是最好的线。
假设有一组真实数据 和模型的预测数据 。我们希望它们越接近越好。（人工智能方向的理解）
定义“误差”： 我们定义每一个点的误差为 （真实值 - 预测值）。
如果直接求和 ，会出问题。因为误差有正有负（有的点在直线上方，有的在下方）。正负相抵后，总和可能为 0，但这并不代表直线完美穿过了所有点，可能只是误差刚好抵消了。
去符号： 为了消除符号影响，我们有两种选择：
取绝对值：
取平方：
为什么选择“平方”而不是“绝对值”？ 这就是最小二乘法的核心智慧，主要有三个原因：
惩罚大误差（关键点） ：
平方会对大的误差进行“严厉惩罚”。如果误差是 2，平方后是 4；如果误差是 10，平方后变成了 100。
这意味着，最小二乘法绝不容忍极端偏差。它会拼命把那条线往离得最远的那个点拉一拉，尽量照顾所有点，不让任何一个点掉队太远。
计算方便（求导） ：
绝对值函数 在 处有一个尖角，不可导，数学处理很麻烦。
平方函数 是光滑的抛物线，处处可导。可以直接求导并令其为 0，一步就能算出答案（解析解）。
统计学背景（高斯分布） ：
如果假设数据的噪声服从正态分布（高斯分布） ，那么“最小二乘法”求解出来的结果，等价于“极大似然估计”。这意味着它是统计学上最合理的估计。（这个后边再专门研究）
‍
代数推导
第一步：定义损失函数
假设模型为 。我们要最小化误差向量 的模长平方（即能量）：
写成矩阵乘法形式（实数域）：
第二步：展开公式
利用矩阵转置规则 和 ：
关键技巧：中间两项是标量（实数）。
是一个 的数。
也是一个 的数。
标量的转置等于它自己，所以这两项是相等的。
第三步：求梯度（对求导）
我们需要用到两个矩阵求导公式：
（针对线性项）
（针对二次项，当 对称时）
对 求导：
对 是常数 。
对 求导 。
对 求导 。
第四步：令导数为 0（极值点）
为了找到最小值，令梯度为 0：
这就是著名的“正规方程” (Normal Equation)。
最后左乘逆矩阵 ：
几何推导：正交投影 (The Geometric Way)
核心思想
列空间：矩阵 的列向量张成了一个子空间（平面），记为 。
限制：无论 怎么取， 只能在这个平面上移动。
目标：真实值 通常不在这个平面上（因为有噪声）。我们要在这个平面上找一个点 ，离 最近。
结论：几何学告诉我们，最近的点就是在平面上的正交投影。
推导过程
这意味着，误差向量 必须垂直（正交） 于这个平面。
既然垂直于平面，那么 必须垂直于平面上的所有基向量（即 的每一列）。
用数学语言描述“垂直”就是内积为 0。
所以， 的每一列与 的点积都为 0：
代入 ：
展开：
图片放的不合适，但是可以作为一个参考吧，具体的看原文。
可以参考https://blog.csdn.net/MoreAction_/article/details/106443383?ops_request_misc=%257B%2522request%255Fid%2522%253A%25227ef8c0683c2dd970ff9fb57930568291%2522%252C%2522scm%2522%253A%252220140713.130102334..%2522%257D&request_id=7ef8c0683c2dd970ff9fb57930568291&biz_id=0&spm=1018.2226.3001.4187这篇文章。
‍
以二维举例：
假设我们在一个 3维空间里。有两个基准向量 和 。它们定义了一个平面。要寻找这两个向量的组合，去够那个飘在空中的点 。
具体的数值例子：
(红色箭头) ：躺在 x 轴上
(蓝色箭头) ：躺在 y 轴上
(空中的星星) ：悬浮在半空
显然， 和 无论怎么组合（），结果的第三个分量永远是 0。这意味着：永远无法用和完美组合出（因为 的高度是 3）。
目标是找到一组系数 ，使得组合出来的向量 （预测值）离 最近。
直觉： 在地板上的垂直投影就是最近的点。
预测向量：。
误差向量：。为了让距离最短，误差线必须垂直于地板。
为什么是“点积为 0”？ 如果误差线 垂直于整个地板，那么它必须垂直于地板上的每一根经纬线。
也就是：
必须垂直于 。
必须垂直于 。
数学翻译（垂直 = 点积为 0） ：
把具体的数字代进去看看：预测值显然应该是影子的坐标 。
那么误差向量就是 （一根竖直向上的线）。
验证垂直性：
（没毛病，垂直）
（没毛病，垂直）
矩阵化：的诞生
把 和 叠在一起，这不就是矩阵 吗？
所以几何条件变成了：。
重点：这里的 作用就是—— “一次性检查误差向量是否垂直于所有的基向量” 。
求解：
将 代入：
展开：
代入数值计算验证一下：
构造 ****：
计算(投影分量) ：
计算(坐标系校正) ：
解方程：
显然：
结论：
我们算出 。
预测值 。
‍
与最大似然估计的联系
简单来说：最小二乘法是最大似然估计在“误差服从正态分布”这个特定假设下的一种特殊情况。
一、 什么是最大似然估计 (MLE)？
Maximum Likelihood Estimation (MLE) 是一种统计方法，用来估计模型的参数。
1. 直观理解：侦探视角
假设你是一个侦探，来到了案发现场（这就是你拿到的数据）。你需要推断凶手是谁、作案手法是什么（这就是模型参数）。
概率（Probability） ：已知参数，预测数据。
例子：已知硬币是均匀的（参数），扔10次，出现5正5反的概率是多少？
似然（Likelihood） ：已知数据，推断参数。
例子：不知道硬币均不均匀，但我扔了10次，全是正面（数据）。请问，这枚硬币“动过手脚”的可能性有多大？
MLE的逻辑：既然我抛出了10次全是正面，那么“这枚硬币两面都是正面”这个参数假设，虽然不一定对，但最能解释当前的数据。
MLE的核心思想：
找到一组参数 ，使得在这组参数下，出现当前数据的概率最大。
二、 最小二乘法与 MLE 的联系（数学推导）
为什么说最小二乘法是 MLE 的一种特例？我们通过线性回归来推导。
1. 设定假设
在线性回归中，假设真实值 和预测值 之间有一个误差 ：
关键步骤来了！ 必须对这个误差 做一个假设。通常假设误差服从高斯分布（正态分布） ，且均值为0，方差为 。
这意味着，给定 和 ，真实标签 也服从正态分布，其均值是 ：
2. 写出概率密度函数
根据正态分布公式，某一个样本点 出现的概率密度是：
3. 写出似然函数 (Likelihood Function)
因为样本之间是独立的，所以所有样本同时出现的概率（似然函数 ）就是把每一个样本的概率乘起来：
4. 最大化似然函数
要找到 让 最大。连乘很难算，为了方便计算，取对数（Log-Likelihood）。因为对数函数是单调增的，最大化对数似然等价于最大化原函数。
利用对数性质 ，展开：
5. 见证奇迹的时刻
要最大化这个 。
第一项 是常数（和 无关），求导时直接为0，可以忽略。
剩下的就是要最大化：
最大化一个负数，等价于最小化那个正数（去掉负号） 。
同时， 也是常数系数，不影响求极值的位置。
所以，最大化似然函数，最终等价于：
最小化
看！这不就是最小二乘法的公式吗？
1. 这种联系说明了什么？
最小二乘法（Least Squares） 是从几何角度（距离最小）或代数角度出发的。
最大似然估计（MLE） 是从概率统计角度出发的。
联系：如果你假设数据中的噪声是正态分布的，那么这两种方法求出来的解是完全一样的。
2. 如果噪声不是正态分布呢？
这就体现出 MLE 的普适性了。
如果误差服从拉普拉斯分布（Laplace Distribution，尖峰更厚），推导出的 MLE 等价于 最小化绝对值误差（L1 Loss） ，也就是 Lasso 回归的基础。
如果误差服从泊松分布（Poisson Distribution），推导出的就是广义线性模型中的泊松回归。
最小二乘法之所以好用，是因为在自然界中，大多数误差确实服从正态分布（中心极限定理）。它是最大似然估计在高斯噪声假设下的完美替身。
‍
Tips：
残差=真实值-预测值
物理、数学在某些意义上的统一，确实牛皮

> 注：原文部分公式与配图以图片呈现，文本中未保留，需要时对照原文查看。
