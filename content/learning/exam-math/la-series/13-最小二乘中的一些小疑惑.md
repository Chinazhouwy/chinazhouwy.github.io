---
title: "最小二乘中的一些小疑惑"
date: "2026-09-01"
domain: "学习"
area: "数学"
module: "线性代数合集"
project: ""
type: "文章"
status: "可复习"
priority: "P1"
energy: "medium"
visibility: "public"
summary: "最小二乘中的一些小疑惑 范数推导 在电磁场（复数域）中推导这个公式，用到的是复向量空间的内积性质和复变函数求导（Wirtinger 导数） 。 目标函数就是： 范数的展开（关键步骤） 在复数空间中，范…"
tags:
  - 数学
  - 向量
  - 最小二乘法
  - 内积与投影
source: "https://mp.weixin.qq.com/s/kn4izuFPINuRk_SyqN9hAw"
album_position: 13
source_url: "https://mp.weixin.qq.com/s?__biz=MzUzNzY3NjEwNQ==&mid=2247484606&idx=1&sn=437addbf1fefddb08bba1d3dda388758&chksm=fae21498cd959d8e7e08645056fc08e6a09ffdc66babb8abea5bf6759ef803af26a8433679a1"
---
# 最小二乘中的一些小疑惑

最小二乘中的一些小疑惑
范数推导
在电磁场（复数域）中推导这个公式，用到的是复向量空间的内积性质和复变函数求导（Wirtinger 导数） 。
目标函数就是：
范数的展开（关键步骤）
在复数空间中，范数的平方定义为向量与自己的内积：。
注意： 电磁学中通常定义内积 （或者是 ，这取决于教材定义，但结果结构一样，这里假设是对第二项取共轭）。
展开过程如下：
根据内积的线性性质（分配律），把它拆成四项：
这里要注意复数系数提出的规则（第一项线性，第二项共轭线性）：
（系数在后一项，提出来要变共轭）
（系数在前一项，直接提）
所以，展开后的完整式子是：
如何求导得到 ？
这是一个关于复数 的实值函数。求最小值的最快方法是利用 Wirtinger 导数（也叫复梯度）。
简单来说，在求极值时，可以把 和 看作两个独立的变量。
为了让 最小，我们需要让 对 的偏导数为 0：
让对上面的展开式求导：
：与 无关
：对 求导
：这一项看作常数（因为它只含 不含 ）
：对 求导
把它们合起来令为 0：
解出
移项：
得到最终结果：
另一种更直观的理解（几何投影）
想用向量 （猜测场）去逼近向量 （真实场）。这就相当于要把 投影到 的方向上。
在线性代数中，向量 在向量 上的投影系数公式就是：
系数两个向量的内积基向量的长度平方
1. 场景设定
想象我们在一个二维平面上（或者高维空间，原理一样）：
向量：这是我们的目标。
向量：这是我们现在的方向。
系数：我们可以把向量 拉长或缩短，变成 。
2. 目标
我们的目标是找到一个 ，使得 这个新向量，离目标最近。也就是让误差向量 的长度（范数）最小。
3. 几何直觉：垂直（正交）原理
请闭上眼睛想象一下（或者看下面的示意描述）：
你站在原点，手中拿着一支手电筒照向方向 。
目标物体 悬在空中，不在手电筒的光柱上。
你想在光柱上找一点（），这点离物体 最近。
结论是： 从 向光柱 做一条垂线，垂足的位置就是距离最近的点。
在数学上，这意味着误差向量必须垂直于方向向量。如果它们不垂直（比如有个锐角），那你就可以沿着 再往前挪一点点，误差就会变得更小，直到垂直为止。
4. 数学推导
“垂直”在数学上的定义就是内积为 0。
误差向量方向向量
展开这个式子：
(注：这里假设实数域方便理解，复数域结论一致)
移项得到：
于是直接得到系数：
与
那么，为什么之前的最小二乘的解是，而范数推导出来的结果却是 ？其实，它们本质上完全是同一个公式！
公式 是最小二乘法的通用矩阵形式（通常用于多元线性回归，比如一次求出几十个参数）。公式 是这个通用公式在只求一个参数时的特例。
如果把通用公式里的矩阵 缩减成只有一列的向量（记为 ），会发生什么神奇的变化。
第一步：代入通用公式
因为我们现在只有一个特征（只求一个系数 ），所以把矩阵 换成向量 ，把 换成 ：
第二步：理解
是一个列向量 。
是一个行向量 。
它们相乘 得到的是一个 的数值（也就是一个标量）。
根据定义，向量自己和自己点乘，就是它的长度（范数）的平方：
第三步：理解求逆
对于矩阵，我们要算逆矩阵。
但对于一个标量（普通数字） ，求逆就是取倒数。
第四步：理解
这就是两个向量的点积（内积）：
(注：在实数域 ** ，在复数域需注意共轭顺序，对应 ** )
第五步：合体
把上面简化的结果拼回去：
整理一下：
总结
是屠龙刀（解决多变量复杂回归）。
是水果刀（解决单变量系数修正）。
