---
title: "离散傅里叶变换"
date: "2026-09-01"
domain: "学习"
area: "数学"
module: "线性代数合集"
project: ""
type: "文章"
status: "可复习"
priority: "P1"
energy: "medium"
visibility: "public"
summary: "雷达传感器进行数字处理的信号可以是采样的基带时域信号，也可以是低频中频（IF）时域信号。这种低频中频时域信号是从毫米波载波频率转换到相对较低的几兆赫兹频率域后进行采样的，例如滤除直流分量或减少噪声对信…"
tags:
  - 数学
  - 傅里叶
source: "https://mp.weixin.qq.com/s/kn4izuFPINuRk_SyqN9hAw"
album_position: 8
source_url: "https://mp.weixin.qq.com/s?__biz=MzUzNzY3NjEwNQ==&mid=2247484201&idx=1&sn=44cd7eae1fbebb160fec3d8bbdff6028&chksm=fae2130fcd959a19cf0953632289a9ef9852782ca01136f0c753e12b4788d4f34b4332bda351"
---
# 离散傅里叶变换

雷达传感器进行数字处理的信号可以是采样的基带时域信号，也可以是低频中频（IF）时域信号。这种低频中频时域信号是从毫米波载波频率转换到相对较低的几兆赫兹频率域后进行采样的，例如滤除直流分量或减少噪声对信号评估的影响。
以采样率 对连续时间信号 进行理想采样，对应于将信号与狄拉克梳函数相乘：
严格来说，信号 的采样精确对应于时间上的离散点 ，其中 。两个样本 和 之间的时间间隔称为采样间隔 ，它对雷达性能的影响将在本章后面更详细地解释。采样信号 （3.1）的傅里叶变换（FT）给出其连续频谱：
后一种表示也称为离散时间傅里叶变换（DTFT）。仔细观察可知，频谱是周期性的，周期为 。因此有：
其中 是基频周期的频谱。因此，采样连续时间信号 频谱的无模糊范围由采样率 或采样间隔 决定。为确保频谱重复不重叠，模拟信号的带宽 应小于采样持续时间的倒数或采样频率：
这一性质就是所谓的奈奎斯特-香农定理或采样定理。
由于连续FT和DTFT都无法高效地进行数字实现和评估，因此通常使用DFT
用于有限离散信号的数字雷达信号处理。与FT和DTFT都产生连续频谱不同，DFT将有限长度的离散时间信号映射到离散周期性频谱。由于这一特性，DFT可以在数字计算机上非常高效地实现。在 对应于2的幂的特殊情况下，快速傅里叶变换（FFT）使用Cooley-Tukey方法（也称为Radix-2或蝶形方法）提供了一种特别快速高效的DFT计算方法。这将DFT的计算复杂度从 降低到FFT的 。
这里主要针对两点进行一个说明
为什么是B小于等于Fs
1. 实信号 vs. 复信号 (核心原因)
现代雷达系统（尤其是FMCW雷达）通常采用 I/Q 解调（正交解调），采样后的数据是复数（由实部 I 和虚部 Q 组成）。
实信号 (Real Signal): 是最熟悉的场景（比如录音）。它的频谱是共轭对称的（正负频率对称）。如果需要的最高频率是 ，那么频谱实际上占据了 到 的范围。为了不让正负频谱打架（混叠），采样率必须能够覆盖这个 的跨度。即 。
复信号 (Complex Signal): 在雷达中，通过 I/Q 通道采样，得到复信号。复信号的频谱不对称。正频率和负频率携带不同的信息。因此，带宽 代表了从最低频到最高频的总宽度。因为不需要像实信号那样“浪费”一半的采样率去照顾对称的负频率，所以采样率只需要大于总带宽即可。即 。
2. 从公式 (3.4) 的几何角度理解
抛开实复信号的区别，仅看的公式 (3.4)：
这个公式告诉我们：采样后的频谱，是原始频谱以为周期进行“复制平移”的结果。
想象一下： 是一个宽度为 的盒子。 是放置这些盒子的间隔距离。
为了让这些平移复制出来的“盒子”不互相重叠（即无混叠），盒子的宽度 ( ) 必须小于盒子的间隔距离 ( ) 。
即：
公式的推导
这个推导过程非常经典，它是数字信号处理的核心基石。公式 (3.4) 的推导主要依赖于信号与系统中的两个重要工具：卷积定理 (Convolution Theorem) 和 狄拉克梳状函数 (Dirac Comb) 的傅里叶变换。
第一步：利用卷积定理
根据公式 (3.1)，采样后的信号 是原始信号 与狄拉克脉冲串 的乘积：
其中， 是单位脉冲串：
卷积定理告诉我们：时域中的乘积，对应频域中的卷积。
因此，采样信号的频谱 等于原始信号频谱 与脉冲串频谱 的卷积：
第二步：求脉冲串 的频谱
这是推导中最关键、也最容易出错的一步（也就是 出现的地方）。
需要求 的傅里叶变换。
由于 是周期为 的周期函数，可以先将其展开为傅里叶级数 (Fourier Series) ：
其中 。计算傅里叶系数 ：
因为 仅在 处有值且积分为 1，且 ，所以积分结果就是 1。
把 代回级数公式，得到 的另一种表达：
现在对这个形式做傅里叶变换。根据频移性质 ，可以得到：
注意： 这里的系数 非常重要，它代表采样后能量的分散，也是公式 (3.4) 前面那个系数的来源。
第三步：计算卷积（频谱搬移）
现在将 Step 2 的结果代入 Step 1 的卷积公式中：
利用卷积的线性性质，把常数和求和符号提出来：
最后，利用 函数的筛选性质（Sifting Property） ：任何函数 与 卷积，结果就是将该函数平移到 处，即 。
所以：
最终得到公式 (3.4)：
‍
