---
title: "相似矩阵中的不变量"
date: "2026-09-01"
domain: "学习"
area: "数学"
module: "线性代数合集"
project: ""
type: "文章"
status: "可复习"
priority: "P1"
energy: "medium"
visibility: "public"
summary: "按照之前的定义，如果两个矩阵 和 相似，意味着存在一个可逆矩阵 ，使得： 那么，相似矩阵的本质又是什么呢？矩阵和代表同一个线性变换，它们在不同坐标系下被观察到。矩阵 充当了不同坐标系下翻译，使其能够相…"
tags:
  - 数学
  - 矩阵
  - 行列式
  - 特征值
source: "https://mp.weixin.qq.com/s/kn4izuFPINuRk_SyqN9hAw"
album_position: 38
source_url: "https://mp.weixin.qq.com/s?__biz=MzUzNzY3NjEwNQ==&mid=2247485638&idx=1&sn=3b2cdfedf47d9c2481b195107254ff5f&chksm=fae218e0cd9591f6e6befb9aefd3e4bcce24b59214cdf7caf09fb3d590ffa0ad691bd24f3ce2"
---
# 相似矩阵中的不变量

按照之前的定义，如果两个矩阵 和 相似，意味着存在一个可逆矩阵 ，使得：
那么，相似矩阵的本质又是什么呢？矩阵和代表同一个线性变换，它们在不同坐标系下被观察到。矩阵 充当了不同坐标系下翻译，使其能够相互理解。
重新看一下相似矩阵的含义：
相似矩阵的特征值相同
如果 和 相似，它们的特征多项式完全相同：
从而导致它们的特征值完全相同，且每个特征值的代数重数和几何重数也完全一致。
已知 ，带入到矩阵 的特征多项式中。因为单位矩阵 满足 ，我们可以把式子里的 巧妙地改写，从而把左边的 和右边的 提取出来：
根据行列式的乘法性质（即多个矩阵相乘的行列式，等于它们各自行列式的乘积，），我们可以把上式完全拆开：
由于标量乘法满足交换律，我们可以把 和 挪到一起。同时，因为 ，它们两者的乘积在数学上会完美地抵消为 1：
特征值代表了一个线性变换在内生主轴上的能量缩放倍数或固有振动频率。不管换到哪个坐标系去观察系统，系统的固有振动频率（共振点）是客观存在的物理事实，绝对不会因为换了把尺子度量就发生改变。
相似矩阵的迹相同
矩阵的迹定义为方阵主对角线所有元素的加和：
如果 与 相似，则 。同时，这个值也严格等于所有特征值的加和：
在线性空间中，矩阵的迹量化了整个空间在变换下的总体线性拉伸总量。虽然变换在不同的坐标轴上分配的拉伸比例会变，但所有方向拉伸的总体积（总能量）是恒定的。
相似矩阵的行列式相同
方阵的行列式在相似变换下保持不变：
同时，行列式的值严格等于所有特征值的乘积：
行列式的几何意义是变换前后的空间体积缩放比例。比如，一个变换把一个单位立方体放大了 5 倍，那么无论你从哪个角度（坐标系）去量这个立方体的体积，它被放大 5 倍的结果是铁打不动的。
相似矩阵的秩相同
两个相似矩阵的秩完全相等：
矩阵的秩代表了变换后空间真正保留下来的维度（即有效输出空间的自由度）。如果一个矩阵具有降维打击的能力，把三维空间压扁成了一个二维面，那么换个坐标系看，它依然是个二维面，损失的维度不会凭空再生。
算是对相似矩阵的一个补充，这些东西其实都是一个共同的目标，把复杂的东西简单化，一个极其复杂、所有元素都填满了数字的非对角矩阵 ，去求它的行列式、迹或者特征值。如果直接去算，计算量会让你崩溃。
但如果这个矩阵能够对角化，即相似于一个清爽的对角矩阵 ：
那么利用不变量的原理，根本不需要去碰矩阵 里面的那堆乱麻。只需要把对角线上的特征值直接相加，就得到了 的迹；直接相乘，就得到了 的行列式。
相似不变量的存在证明了：在线性代数看似混乱的坐标变换表象下，系统内部永远保留着一套不可动摇的拓扑骨架。
