---
title: "MySQL常见⾯试题"
date: "2024-09-26"
domain: "学习"
area: "技术"
module: ""
project: ""
type: "文章"
status: "可复习"
priority: "P1"
energy: "medium"
visibility: "private"
summary: "MySQL常见⾯试题"
tags:
---

# MySQL常见⾯试题

> **来源**: [小红书](https://www.xiaohongshu.com/explore/66f52f5a000000001a02246a)
> **发布日期**: 2024-09-26
> **作者**: Java面试
> **标签**: `#java` `#java学习` `#mysql` `#java项目` `#javaweb` `#后端开发`
> **考点分类**: char vs varchar / 事务四大特性 / 索引结构 / 聚簇索引 / 回表查询 / B+tree / 索引优化 / 慢SQL定位 / SQL优化

---

## 目录（共10页）

### 1、基本知识
- 1.1 MySQL数据库中的 char 与 varchar的区别是什么？
- 1.2 什么是事务以及事务的四大特性？

### 2、索引基础
- 2.1 MySQL数据库索引的数据结构？
- 2.2 知道什么是聚簇索引，什么是二级索引吗？
- 2.3 什么是回表查询？
- 2.4 为什么MySQL索引结构是B+tree？

### 3、索引优化
- 3.1 索引创建的原则？
- 3.2 简单聊聊索引失效的场景？

### 4、SQL性能分析及优化
- 4.1 如何定位出项目中的慢SQL？
- 4.2 在项目中如何知道SQL语句的执行性能，以及索引是否生效？
- 4.3 谈谈你在SQL优化方面的经验？

---

## 1、基本知识

### 1.1 MySQL数据库中的 char 与 varchar的区别是什么？

MySQL中的 char 和 varchar 都是用于存储字符串的数据类型，但它们在存储方式和性能上有所不同。

**第一点：存储方式不同**
- **char**：定长字符串，长度是固定的，不管实际存储的字符串长度如何，都会占用固定长度的存储空间。如：char(10) 会始终占用10个字符的空间。
- **varchar**：变长字符串，长度不固定。占用的空间与实际存储的字段长度有关。如：varchar(10) 表示最多可以存储10个字符，如果存储的字符串长度不足10，假设为5，只会占用5个字符空间。

**第二点：性能不同**
（详见原文后续内容）

---

## 2、索引基础

### 2.1 MySQL数据库索引的数据结构？
（B+Tree、Hash等）

### 2.2 知道什么是聚簇索引，什么是二级索引吗？
（聚簇索引=主键索引，叶子节点存完整行数据；二级索引叶子节点存主键值，需要回表）

### 2.3 什么是回表查询？
（通过二级索引找到主键后，再根据主键去聚簇索引中查找完整数据）

### 2.4 为什么MySQL索引结构是B+tree？
（B+Tree的特点：多路平衡、叶子节点链表、范围查询友好、磁盘IO少）

---

## 3、索引优化

### 3.1 索引创建的原则？
（最左前缀原则、选择性高的列、避免过多索引、覆盖索引等）

### 3.2 简单聊聊索引失效的场景？
（不满足最左前缀、对索引列使用函数、隐式类型转换、OR条件、LIKE以%开头等）

---

## 4、SQL性能分析及优化

### 4.1 如何定位出项目中的慢SQL？
（慢查询日志、EXPLAIN分析、SHOW PROFILE）

### 4.2 在项目中如何知道SQL语句的执行性能，以及索引是否生效？
（EXPLAIN的type、key、rows、Extra字段解读）

### 4.3 谈谈你在SQL优化方面的经验？
（索引优化、SQL改写、分页优化、JOIN优化等）
